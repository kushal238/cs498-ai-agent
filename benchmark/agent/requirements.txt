# Agent container dependencies only.
# Used by agent/Dockerfile — kept minimal to avoid disk-space issues during build.
# scoring/test libs excluded; scoring runs on the host (harness).
openai>=1.0.0
pydantic>=2.0.0
jsonschema>=4.20.0
requests>=2.31.0
numpy>=1.26.0
