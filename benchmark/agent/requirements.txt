# Agent container dependencies only.
# Used by agent/Dockerfile — kept minimal to avoid disk-space issues during build.
# sentence-transformers and other scoring/test libs are intentionally excluded;
# scoring runs on the host (harness) and never inside the agent container.
langgraph>=0.1.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
jsonschema>=4.20.0
requests>=2.31.0
numpy>=1.26.0
