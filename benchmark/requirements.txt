# Host-side dependencies (harness, scoring, tests).
# Install on the host before running the benchmark:
#   pip install -r benchmark/requirements.txt
#
# Do NOT use this file for the agent Docker container — use agent/requirements.txt instead.
langgraph>=0.1.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
jsonschema>=4.20.0
requests>=2.31.0
rouge-score>=0.1.2
scikit-learn>=1.4.0
numpy>=1.26.0
sentence-transformers>=3.0.0
bert-score>=0.3.13
transformers<5
pytest>=7.4.0
